import { SidebarWrapper } from '@/components/shared/sidebar';

const RightSidebar = () => {
  return <SidebarWrapper className="right-0 border-l max-xl:hidden">RightSidebar</SidebarWrapper>;
};

export default RightSidebar;
